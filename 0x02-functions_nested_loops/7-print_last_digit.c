#include "main.h"

/**
 * print_last_digit - function that prints the last
 * digit of a number
 * @n: integer to affect.
 * Return: value of last digit
 */

int print_last_digit(int n)
{
	int  last;

	last = n % 10;

	return (last);
}
