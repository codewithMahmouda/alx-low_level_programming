#include "main.h"

/**
 * mul - This is a function that multiplies two integers.
 * @a: character to multiply.
 * @b: character to multiply.
 * Return: value of a * b.
 */

int mul(int a, int b)
{
	return (a * b);
}
